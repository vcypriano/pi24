import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SolicitarenergiaPage } from './solicitarenergia.page';

describe('SolicitarenergiaPage', () => {
  let component: SolicitarenergiaPage;
  let fixture: ComponentFixture<SolicitarenergiaPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(SolicitarenergiaPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
